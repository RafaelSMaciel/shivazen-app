{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn shivazen.wsgi --bind 0.0.0.0:$PORT --workers 4 --threads 2 --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/healthz/",
    "healthcheckTimeout": 60
  }
}
