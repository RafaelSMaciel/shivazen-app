{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "sh -c 'python manage.py migrate --noinput && python manage.py collectstatic --noinput --verbosity 0 && exec gunicorn shivazen.wsgi --bind 0.0.0.0:${PORT:-8080} --workers 1 --threads 4 --timeout 120 --access-logfile - --error-logfile -'",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/healthz/",
    "healthcheckTimeout": 60
  }
}
